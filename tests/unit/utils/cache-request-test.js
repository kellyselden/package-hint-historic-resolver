import cacheRequest from '../../../utils/cache-request';
import { module, test } from 'qunit';

module('Unit | Utility | cache request');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = cacheRequest();
  assert.ok(result);
});
