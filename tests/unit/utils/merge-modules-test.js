import mergeModules from '../../../utils/merge-modules';
import { module, test } from 'qunit';

module('Unit | Utility | merge modules');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = mergeModules();
  assert.ok(result);
});
