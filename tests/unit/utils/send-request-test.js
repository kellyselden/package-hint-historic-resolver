import sendRequest from '../../../utils/send-request';
import { module, test } from 'qunit';

module('Unit | Utility | send request');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sendRequest();
  assert.ok(result);
});
